<!DOCTYPE html>
<html>
  <head>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>

    <title>TheGossipProject</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">

    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"> 

  <link href="https://afeld.github.io/emoji-css/emoji.css" rel="stylesheet">


  </head>
  <body cz-shortcut-listen="true">

    <div class="fixed-top d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-normal">Gossip Project</h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <%= link_to'Acceuil', welcome_path %>
        <%= link_to'Contact', static_page_contact_path %>
        <%= link_to'Team', static_page_team_path %>
        <%= link_to'Gossips', gossips_path %>

         <% if session[:user_id].nil? %>

        <%= link_to "Se Connecter", new_session_path %>
        <% end %>
        
      <% if session[:user_id] %>
        <%= link_to "Se déconnecter", session_path(session), method: :delete %>
        <% end %>

      </nav>
    </div>

    <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center"><br>
      <p><% if session[:user_id] %></p>
      <p>Connecté en tant que :<%= User.find_by(id:session[:user_id]).first_name %></p>
      <% end %>
      <h1 class="display-4"></h1>
      <p class="lead"></p>
    </div>


<%= yield %>
</body>
</html>
